version: 1.1.5

endpoints:
  custom:
    - name: "HuggingFace"
      apiKey: "${HUGGINGFACE_TOKEN}"
      baseURL: "https://api-inference.huggingface.co/v1"
      models:
        fetch: true
      dropParams: ["top_p"]
